title: Month

pages:
  sort: flip
  template:
    - article.text
    - article.link
    - article.video
files: false

fields:

  pageMeta:
    label: Page Meta
    type: headline
  title:
    label: Title
    type:  text